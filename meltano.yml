version: 1
send_anonymous_usage_stats: true
project_id: tap-dv360
default_environment: test
environments:
- name: test
state_backend:
  uri: gs://uowaikato-main-meltano-state-staging
plugins:
  extractors:
  - name: tap-dv360
    namespace: tap_dv360
    pip_url: -e .
    capabilities:
    - state
    - catalog
    - discover
    - about
    - stream-maps
    config:
      start_date: ${START_DATE}
      advertiser_id: ${ADVERTISER_ID}
      query_standard: 'query_standard.json'
      query_youtube: 'query_youtube.json'
      service_account: ${SERVICE_ACCOUNT}
  loaders:
  - name: target-csv
    variant: meltanolabs
    pip_url: git+https://github.com/MeltanoLabs/target-csv.git
  - name: target-jsonl
    variant: andyh1203
    pip_url: target-jsonl
    config:
      output_dir: ./output
  - name: target-bigquery
    variant: z3z1ma
    pip_url: git+https://github.com/z3z1ma/target-bigquery.git
    config:
      project: "uowaikato-main"  # Replace with your actual GCP project ID
      dataset: "dv360"   # Replace with your BigQuery dataset name